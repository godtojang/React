const Address = (props) => {
  return (
    <div>
      <span style={{ fontWeight: "bold", color: "05141f" }}>
        {props.address}
      </span>
    </div>
  );
};

export default Address;

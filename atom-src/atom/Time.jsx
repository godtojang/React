const Time = (props) => {
  return (
    <div>
      <span style={{ fontSize: "18px", fontWeight: "bold", color: "#05141f" }}>
        {props.time}
      </span>
    </div>
  );
};

export default Time;
